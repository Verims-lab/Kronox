# CORE_PROMPT Compatibility Pointer

Status: compatibility pointer.

This file is intentionally short. Older prompts and tools may still ask for
`CORE_PROMPT.md`, but the canonical operational rules for Kronox live in:

```text
KRONOX_CORE_PROMPT.md
```

Product identity, game feel, and visual language live in:

```text
Kronox.md
```

Runtime Health/doc mirrors used by the app live in:

```text
src/lib/healthAlignmentDocMirrors.js
```

Keep this pointer only for compatibility. Do not duplicate the full canonical
prompt here.
