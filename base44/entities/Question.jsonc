{
  "name": "Question",
  "type": "object",
  "description": "Codex168 \u2014 Target schema for the new Kronox question dataset. Current gameplay receives minimal compatibility fields from the authenticated getQuestions fetch layer; direct Question entity reads are admin-only so the full question bank is not publicly exposed.",
  "properties": {
    "id": {
      "type": "number",
      "description": "Unique incremental question ID from the new dataset."
    },
    "question": {
      "type": "string",
      "description": "The question text"
    },
    "answer": {
      "type": "string",
      "description": "Answer text. Usually a year such as \"2007\" today; may later include month/year such as \"Ocak 2007\"."
    },
    "main_category_id": {
      "type": "number",
      "enum": [
        1,
        2,
        3,
        4,
        5,
        6
      ],
      "description": "Primary category ID. References Category.category_id."
    },
    "second_category_id": {
      "type": "number",
      "enum": [
        1,
        2,
        3,
        4,
        5,
        6
      ],
      "description": "Optional future secondary category ID. References Category.category_id if present."
    },
    "third_category_id": {
      "type": "number",
      "enum": [
        1,
        2,
        3,
        4,
        5,
        6
      ],
      "description": "Optional future tertiary category ID. References Category.category_id if present."
    },
    "sub_category": {
      "type": "string",
      "description": "Optional free-text subcategory for the future question dataset."
    },
    "tag": {
      "type": "string",
      "description": "Optional free-text tags, comma-separated if needed."
    },
    "region": {
      "type": "string",
      "description": "Optional geographic/cultural scope such as global, turkey, or europe."
    },
    "difficulty": {
      "type": "number",
      "enum": [
        1,
        2,
        3,
        4,
        5
      ],
      "default": 1,
      "description": "Difficulty level for the future dataset: integer 1 to 5. Current admin tooling still creates 1 to 3 until gameplay/content tooling is intentionally updated."
    },
    "state": {
      "type": "string",
      "enum": [
        "A",
        "P"
      ],
      "default": "A",
      "description": "Content status. A = Active, P = Passive. Authenticated gameplay fetches only active questions through getQuestions."
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional free-text description for this question (up to 1000 characters)."
    }
  },
  "required": [
    "id",
    "question",
    "answer",
    "main_category_id",
    "difficulty",
    "state"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}